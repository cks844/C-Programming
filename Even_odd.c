#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("Enter a number to check:");
    scanf("%d",&num);
    if(n%2==0)
        printf("\t%d is an even number",num);
    else
        printf("\t%d is an odd number",num);
    return 0;
}
